import type { State } from '../types/api';
export function DebugPanel({state, ws}:{state:State;ws:string}) { const values=[['Step',state.step],['Runtime',state.running?'RUNNING':'PAUSED'],['OASIS',state.oasisStatus],['vLLM',state.llmStatus],['WebSocket',ws],['Active',`${state.activeAgentIds.length} / 20`],['LLM requests',state.llmRequestCount],['Events',state.totalEvents],['Last tick',state.lastTickDurationMs ? `${state.lastTickDurationMs} ms` : '—'],['Errors',state.errorCount]]; return <aside className="debug"><div className="panel-label"><span>DEBUG / RUNTIME</span></div>{values.map(([key,value])=><div className="metric" key={key}><span>{key}</span><b>{value}</b></div>)}{state.error && <p className="error">{state.error}</p>}</aside> }
