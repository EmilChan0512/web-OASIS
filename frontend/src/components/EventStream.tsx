import type { Event } from '../types/api';
export function EventStream({events}:{events:Event[]}) { return <section className="events-panel"><div className="panel-label"><span>LIVE EVENT STREAM</span><small>{events.length} retained</small></div><div className="event-list">{[...events].reverse().map(event => <article className="event" key={event.id}><div><b>STEP {event.step}</b><time>{new Date(event.timestamp).toLocaleTimeString()}</time></div><strong>{event.actorName}</strong> <span className="action">{event.actionType.replaceAll('_',' ')}</span>{event.targetAgentName && <> → {event.targetAgentName}</>} {event.content && <p>“{event.content}”</p>}</article>)}{!events.length && <p className="empty">No real OASIS actions yet. Start the runtime after OASIS and vLLM are connected.</p>}</div></section> }
